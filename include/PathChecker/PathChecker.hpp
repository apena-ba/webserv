#ifndef PATH_CHECKER
#define PATH_CHECKER

#include <iostream>
#include <vector>
#include <fstream>
#include "../Configuration.hpp"

class PathChecker {
public:
    static int checkPath(std::string path) {

    }
};

#endif